# Project-1

Personal Website

## Live Preview in Codespaces

1. Install dependencies:
	`npm install`
2. Start live preview:
	`npm run dev`
3. Open the forwarded port `5500` in Codespaces.

The page will auto-refresh when `index.html`, `style.css`, or `script.js` changes.
