{
  "name": "project-1",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "live-server --port=5500 --host=0.0.0.0 --open=index.html --watch=./"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
